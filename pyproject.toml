[project]
name = "rust-shield"
version = "0.1.0"
description = "Security defaults middleware for WSGI applications."
readme = "README.md"
requires-python = ">=3.8"
[tool.pytest.ini_options]
addopts = "-q"
